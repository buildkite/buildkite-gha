agents:
  queue: "hosted"

steps:
  - label: ":white_check_mark: Upload-artifact settled"
    key: "upload-artifact-complete"
    depends_on:
      - step: "gha-upload-artifact"
        allow_failure: true
    command: "echo 'Upload-artifact job settled before API verification'"
