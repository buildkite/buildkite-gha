agents:
  queue: "hosted"

steps:
  - label: ":white_check_mark: Dockerfile action dependency extension"
    key: "dockerfile-action-complete"
    depends_on:
      - step: "gha-docker-action"
        allow_failure: true
    command: "echo 'Dockerfile action settled before native continuation'"
