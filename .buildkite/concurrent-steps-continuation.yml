agents:
  queue: "hosted"

steps:
  - label: ":white_check_mark: Concurrent steps proof complete"
    key: "concurrent-steps-complete"
    depends_on:
      - step: "gha-observe"
        allow_failure: true
    command: "echo 'All concurrent steps proof jobs settled before native continuation'"
