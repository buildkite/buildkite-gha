agents:
  queue: "hosted"

steps:
  - label: ":memo: Summary annotation importer"
    key: "summary-annotation-importer"
    timeout_in_minutes: 15
    retry:
      automatic: false
    plugins:
      - mise#a5845c5082d3a4fe36dd77ae74973dfc86fc91a2:
          version: "2026.5.12"
    command: |
      set -euo pipefail
      commit="$${COMPATIBILITY_PROOF_COMMIT:-$${SMOKE_COMMIT:-$${BUILDKITE_COMMIT:?BUILDKITE_COMMIT is required}}}"
      scripts/verify-source-checkout "$$commit"
      test -z "$$(git status --porcelain --untracked-files=all)"
      distribution_root="$$(mktemp -d "$${TMPDIR:-/tmp}/buildkite-gha-summary-annotation.XXXXXXXX")"
      trap 'rm -rf -- "$$distribution_root"' EXIT
      runtime_version="0.0.0-summary-annotation.$$commit"
      CGO_ENABLED=0 mise exec -- go build -trimpath -buildvcs=false -ldflags "-X main.version=$$runtime_version" -o "$$distribution_root/buildkite-gha" ./cmd/buildkite-gha
      "$$distribution_root/buildkite-gha" upload \
        --event-path testdata/smoke/events/push.json \
        --runner-queue ubuntu-latest=hosted \
        testdata/smoke/.github/workflows/summary-annotation.yml

  - label: ":pipeline: Summary annotation continuation loader"
    key: "summary-annotation-continuation-loader"
    depends_on: "summary-annotation-importer"
    allow_dependency_failure: true
    command: "buildkite-agent pipeline upload .buildkite/summary-annotation-continuation.yml"
