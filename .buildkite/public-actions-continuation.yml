agents:
  queue: "hosted"

steps:
  - label: ":white_check_mark: Public actions proof complete"
    key: "public-actions-complete"
    depends_on:
      - step: "gha-public-actions"
        allow_failure: true
    command: "echo 'Public actions proof jobs settled before native continuation'"
