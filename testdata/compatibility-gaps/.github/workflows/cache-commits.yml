name: Compatibility gap - cache commits
on: push
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/cache@v2
        with:
          path: .buildkite
          key: compatibility-gap
