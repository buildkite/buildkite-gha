name: Compatibility gap - mapped secret forwarding target
on:
  workflow_call:
    secrets:
      compatibility-gap-token:
        required: true
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo compatible
