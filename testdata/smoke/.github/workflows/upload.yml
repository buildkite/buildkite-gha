name: buildkite-gha upload smoke

on:
  push:
  workflow_dispatch:

permissions: {}

jobs:
  bootstrap:
    runs-on: ubuntu-latest
    steps:
      - name: Prove checkout-free shell dispatch
        shell: bash
        run: printf '%s\n' 'PHASE2_UPLOAD_OK'
